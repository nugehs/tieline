node_modules/
*.log
.DS_Store
dist/

# Real specs used only as local test inputs — never commit into the tool.
# (the committed fixture lives at examples/openapi-fixture/spec.json)
examples/*-openapi.json

# Generated reports
tieline-report.html
tieline-report.png

# repoctx generated context (contains machine-local absolute paths)
.dev-context/
.claude/
